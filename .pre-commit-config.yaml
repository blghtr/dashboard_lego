repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
        args: ['--maxkb=1000']
      - id: check-merge-conflict
      - id: debug-statements
      - id: check-ast
      - id: check-docstring-first
      - id: check-json
      - id: pretty-format-json
        args: ['--autofix', '--indent=2']
      - id: mixed-line-ending

  - repo: https://github.com/psf/black
    rev: 24.3.0
    hooks:
      - id: black
        language_version: python3
        files: ^(src/dashboard_lego|tests|examples)/.*\.py$

  - repo: https://github.com/pycqa/isort
    rev: 5.13.2
    hooks:
      - id: isort
        args: ["--profile", "black", "--line-length=88"]
        files: ^(src/dashboard_lego|tests|examples)/.*\.py$

  - repo: https://github.com/pycqa/flake8
    rev: 7.0.0
    hooks:
      - id: flake8
        args:
          - --max-line-length=88
          - --extend-ignore=E203,W503,E501
        files: ^src/dashboard_lego/.*\.py$

  # Note: mypy is slow and can conflict with installed package
  # Type checking is done in CI instead
  # Uncomment if you want to run mypy locally:
  # - repo: https://github.com/pre-commit/mirrors-mypy
  #   rev: v1.8.0
  #   hooks:
  #     - id: mypy
  #       additional_dependencies: [types-requests]
  #       args: [--ignore-missing-imports, --no-strict-optional]
  #       files: ^(blocks|core|presets|utils)/.*\.py$
  #       exclude: ^tests/
